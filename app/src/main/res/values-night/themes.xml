<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Night (dark) variant: black preview window matching the dark app background. -->
    <style name="Theme.MinePlayer" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/mr_black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
